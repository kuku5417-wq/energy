# energy — 시운전 에너지 측정 로드맵 (문서 전용 repo)

앱이 아니라 **문서만 있는 repo** 다. 시운전 기간 중 호선이 쓰는 **유류·전력을 측정해
총 에너지 비용(전기요금 + 유류비)을 줄이기 위한** 로드맵을 담는다.
공통지침(`code_N/CLAUDE.md`)을 따르되 아래는 이 repo 고유 규칙이다.

> Python·parquet·`.env`·포트 규약은 **해당 없음**. `.gitignore` 의 시크릿·parquet 제외만 유지한다.

## 파일 — 무엇이 정본인가

| 파일 | 역할 | 디자인 세트 |
|---|---|---|
| `01_시운전에너지모니터링_로드맵_v0.6.md` | **정본.** 근거·수치·단계 정의가 전부 여기 있다 | — (`refdoc/DOC_GUIDE.md` 기획안 골격) |
| `02_시운전에너지_수집로드맵.html` | 실무 상세 스윔레인. **01 §6 매트릭스의 파생물** | `shi-blue` |
| `03_시운전에너지_로드맵_경영보고.html` | 경영 보고본. 표지 + 슬라이드 5장, A4 가로 인쇄 | `ppt-executive` |

- **내용이 바뀌면 01 을 먼저 고치고 02·03 을 다시 맞춘다.** 파생물만 고치면 곧 갈린다.
- 02 와 03 은 **세트가 다르다** — 섞어 참조하지 않는다(공통지침의 디자인 세트 규칙).
  03 은 `ppt-executive` 의 "슬라이드당 메시지 1개 · 액센트는 mint 또는 amber 하나만 ·
  mint/amber 원색은 면 전용(텍스트는 mint-text/amber-text) · slate-soft 텍스트 금지"를 지킨다.
- 버전은 파일명 접미사(`_v0.6`)로 올린다. 구버전 파일은 **남기지 않는다**(git 이 이력을 갖는다).

## 내용 뼈대 — 고치기 전에 알아야 할 것

- **6종 데이터** — "얼마나 / 어디에 / 왜" 세 덩어리다.

  | | # | 항목 | 답하는 것 |
  |---|---|---|---|
  | 얼마나 | ① | 유류 사용량 | 연료 L |
  | | ② | **전기 사용량** | 선내 **총 부하** kW·kWh |
  | | ③ | **급전 원천** | 그 전기가 **육전인가 발전기인가** |
  | 어디에<br>(소비처 분석) | ④ | 가동 장비 | **무엇을** 돌렸나 |
  | | ⑤ | **운전방법** | 그 기기를 **어떻게** 운전했나 |
  | 왜 | ⑥ | 발전기 가동 사유 | 발전기를 **왜** 돌렸나 |

- ⚠ **②와 ③을 별개의 계측으로 되돌리지 말 것.** ICMS/PMS 가 보는 것은 **선내 총 부하 하나**이고,
  육상에서 받았든 발전기가 만들었든 **같은 데이터**다. 구분은 **발전기 ACB 연결 상태**로 된다.
  그래서 **발전기 쪽에 계측기를 따로 달지 않는다.**
- ⚠ **⑤ 운전방법은 급전 원천(육전/발전기 절체)이 아니다.** 발전기가 아닌 **다른 기기(소비처)를
  어떻게 운전했나**다. **1차는 logbook, 2차는 ICMS 데이터 분석(검토 과제)** 이며
  **1차 입력 항목도 2차 방법도 아직 미정**이다 — **코드 목록을 임의로 만들지 않는다.**
- **절감 레버가 둘이고 방향이 반대다.** 이 구분이 문서 전체의 축이다.

  | | 레버 A — 평시 | 레버 B — 피크 |
  |---|---|---|
  | 싼 쪽 | **육전** (발전기 가동은 손해) | **발전기** (조선소 피크를 낮춤) |
  | 절감 | 불필요 가동 축소 → 유류비 | 피크 인하 → 기본요금(12개월) |
  | 전제 | ③⑥ 만으로 가능 — **수요전력 연동 불필요** | 가설 측정기 + 수요전력 연동 필요 |
  | 탄소 | 감소 | 증가 |

- **레버 A 는 "낭비가 있다"고 단정하지 않는다.** 불가피한 가동(발전기 검사·부하시험)은
  호선 이벤트로 식별되므로(`TRIAL`·`LOAD_TEST`·`SHORE_NA`), 그것을 걷어낸 **나머지(`ETC`)를
  드러내는 것**이 2026 산출물이고 줄일 여지는 그다음에 판단한다. 이 문장을 약속으로 바꾸지 말 것.
- 단계: **1단계** 현재 → **2단계** 2026(logbook + 가설 shore connection 측정기 + 수요전력 연동)
  → **3단계** 2027(평시 절감 + 피크 시범 · ICMS 태그 확정·협의) → **4단계** 2028(ICMS 필수 태그 연결)
  → **표준** 2029–30
- **PMS·ICMS 는 전 호선 기본 사양**이다 — 4단계는 계측기를 다는 일이 아니라 **연결**하는 일이다.
  벤더 협의는 **영업 경유**, 당사 표준 사양으로 관리한다. 어떤 태그를 뽑을지는 **당사 숙제**.
- ⚠ **SVESSEL 로 경로를 가르지 말 것.** 미설치 호선이 있고 대부분 **Kongsberg ICMS** 다. 설치됐어도
  **늦게 살리면 시운전 초기가 누락**된다. 유무로 경로가 갈리면 수집 코드가 두 벌이 되어 드리프트가 쌓인다.
  → **기종·SVESSEL 유무와 무관하게 ICMS 필수 태그 연결 단일 경로.** SVESSEL 은 있으면 보조.
  호선 대장에 SVESSEL 유무·ICMS 기종을 적되, 그것은 **경로가 아니라 태그 매핑·협의 대상**을 가른다.
- **가설 계측기(shore connection 측정기)는 설치·철거·인도 전 회수 공수가 계속 든다.**
  2026 에 먼저 쓰는 이유는 데이터 공백을 막기 위해서고, **4단계 ICMS 연결로 철수하는 것이 전제**다.
- **⑥ 가동 사유는 끝까지 수기로 남는다** — 왜 돌렸는지는 사람만 안다. logbook 은 없어지지 않는다.
- 조선소 수요전력(한전 15분)은 **이미 시스템이 있다** — 연동만 한다. 새로 만들지 않는다.

## 쓰기 규칙

- `[사실]` / `[추정]` / `[미확인]` 태그를 유지한다. **모르는 수치를 지어내지 않는다** —
  절감액은 2단계 기준선 확보 후 3단계에서 실측하며, 그 전까지는 추정치를 제시하지 않는다.
- 숫자에는 기준을 붙인다(무엇 대비, 언제 기준).
- 개인 실명을 적지 않는다(직책으로). UI·문서 텍스트는 한국어.
- 경영 보고본은 **쉬운 단어·짧은 문장**. `SVESSEL`·`ICMS`·`PMS` 같은 약어는 풀어 쓰거나 최소화한다
  (실무 문서 01 에서는 그대로 쓴다).

## 연결된 앱

로드맵이 지목하는 실행 주체는 이 repo 밖에 있다 — 수정은 각 repo 에서 한다.

| 앱 | 이 로드맵에서의 역할 |
|---|---|
| `log` (SSIMS) | **logbook** — 수기 4종(③④⑤⑥)의 입력 창구. 2단계 핵심 |
| `data_manager_F` | parquet 단일 생산자. `fuel_daily`·`power_ts`·`gen_run`·`tariff`(가칭) |
| `esg` | 결과 표시. 전기 축(Scope 2·전기비) + 총비용 합산 뷰 — 데이터가 생긴 3단계 이후 |
| `process` | ⑥ 시운전 항목 자동 태깅의 시각 축 (4단계) |
| `costplan` / `pjtcost` | 실측 원단위를 경비 산출에 연결 (표준 단계) |
| **ICMS 수신기** (신규 `*_collector`) | 4단계 실행 주체. 사내망 상주, 호선별 태그 매핑을 갖고 ICMS 에서 필수 태그를 받는다 |
